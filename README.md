# resume

a simple mac terminal like resume

## Usage

```shell
# clone the repo
git clone https://github.com/shadeofgod/resume.git

# install dependcies
npm install

# run dev server
npm run dev
```
